<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<UserControl
    x:Class="FireTaskMan.Controls.ProcessControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FireTaskMan.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="{x:Bind Process.Name}" FontSize="20" FontWeight="Bold" />

        <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right">
            <ComboBox Name="PriorityComboBox" ItemsSource="{x:Bind Process.Priority}" 
                      SelectedItem="{x:Bind Process.Priority, Mode=TwoWay}" 
                      SelectionChanged="PriorityComboBox_SelectionChanged"
                      Width="100" Margin="8" />

            <Button Content="Kill" Click="Button_Click" Margin="8" />
        </StackPanel>
    </Grid>
</UserControl>
